.projects.screen(projects ng-controller="projectsCtrl")
  .projectList
    p.heading Some projects we worked on...
    div.animate-list(ng-repeat="project in projects")
      .project(ng-mouseover="$parent.viewproject=$index") 
        a(ng-href="{{project.url}}" target="blank")
          |{{project.title}}
  .singleproject.animate-projects(ng-repeat="project in projects" ng-show="$index==viewproject" )
    .projectImage
      a(ng-href="{{project.url}}" target="blank")
        img.largeImage(ng-src="{{project.img[0].url}}" ng-show="project.img[0].url" alt="4real project {{project.description}}")
        img.phone(ng-src="{{project.iphone[0].url}}" ng-show="project.iphone[0].url && $index==viewproject" alt="4real mobile project {{project.description}}")
    //- | {{project.title}}
    | {{viewproject.description}}
